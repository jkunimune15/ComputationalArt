# ComputationalArt
A program that creates randomly generated pretty pictures. You will need numpy and the Python Imaging Library to use it.

To generate new art, run
`$ python recursive_art.py`
from the command line.

To play with genetic algorithms, run
`$ python genetic_paining.py`
.

To draw a Levy C curve, run
`$ python turtles.py`
